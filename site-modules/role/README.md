# role module

Node-purpose composition for SASD systems.

Milestone 2 provides `role::baseline`, which contains
`profile::baseline`. Roles remain free of direct package, file, service, user,
or command resources.
