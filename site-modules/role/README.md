# Roles module placeholder

This directory is reserved for the organization-specific `role` module.

Roles will compose profiles into the purpose of a node. A managed node should normally receive one role. Productive classes and node classification are intentionally deferred to later stepstones.
