# @summary Namespace class for SASD implementation profiles.
#
# The class intentionally manages no resources. Concrete technical components
# belong in focused classes below the `profile::` namespace. Role classes may
# compose those profiles, but profiles must not include roles.
#
# @api private
class profile {
}
