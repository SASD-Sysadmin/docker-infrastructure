---
# Global Hiera data for the environment.
#
# Milestone 1 deliberately defines no lookup keys. Productive values are added
# together with the profile parameter that consumes them and with tests that
# document the expected merge behaviour.
lookup_options: {}
