# Role data placeholder

This directory is reserved for a later role-data hierarchy. It is deliberately
not active in `hiera.yaml` during Milestone 2 because no authoritative custom
role fact or server-side classifier exists yet. Roles are currently selected in
code through `manifests/site.pp`.
