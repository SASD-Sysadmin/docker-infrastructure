# Node-specific data

Use this directory only for justified exceptions keyed by the trusted Puppet certificate name. Prefer common, operating-system, and role data because excessive per-node data weakens consistency.

Example future filename: `server01.example.net.yaml`.
