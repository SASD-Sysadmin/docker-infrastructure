---
# Debian 13 (Trixie) is supported with the distribution Puppet 8.10 agent.
# No release-specific package overrides are currently required.

# .NET 10 SDK repository trust strategy.
profile::dotnet_sdk::repository_strategy: 'microsoft'
