# Puppet environment configuration for this control repository.
# External modules are deployed to ./modules by r10k. Organization-specific
# role and profile modules are stored in ./site-modules.
modulepath = site-modules:modules

# A reviewed config_version helper will be introduced with the Puppet Server
# milestone. It is intentionally omitted from the workload-free initial commit.
