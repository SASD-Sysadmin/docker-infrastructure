---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  # Exceptional, narrowly reviewed per-node overrides. The trusted certificate
  # name also works when this repository is later compiled by Puppet Server.
  - name: Per-node data
    path: "nodes/%{trusted.certname}.yaml"

  # Exact platform data takes precedence over generic operating-system data.
  - name: Operating system and major release
    path: "os/%{facts.os.name}/%{facts.os.release.major}.yaml"

  - name: Operating system
    path: "os/%{facts.os.name}.yaml"

  - name: Operating-system family
    path: "os/family/%{facts.os.family}.yaml"

  - name: Common data
    path: common.yaml
