# This is the main manifest for the Puppet environment.
#
# The initial repository intentionally declares no nodes, classes, or resources.
# Productive classification will be introduced in a later stepstone after the
# roles-and-profiles model and the source of the sasd_role fact are approved.
