# Lifecycle examples

These examples show the node-data shape for active, maintenance, and retirement
states. Copy values into `data/nodes/<exact-certname>.yaml`; do not copy example
filenames into production.
