# Tests

This directory is reserved for the future validation strategy.

Planned layers may include:

- static Puppet and YAML validation;
- unit tests for roles and profiles;
- catalog compilation with representative facts;
- local integration tests in disposable virtual machines or containers where appropriate;
- idempotence checks;
- Puppet Server and r10k deployment tests.

No test framework is selected or installed in the initial commit.
